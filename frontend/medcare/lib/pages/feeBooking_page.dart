// feeBooking_page.dart
import 'package:flutter/material.dart';

class FeeBookingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Prenotazione a Pagamento'),
      ),
      body: Center(
        child: Text('Questa è la pagina per la prenotazione a pagamento.'),
      ),
    );
  }
}
